@extends('layouts.app')
@section('content')<div class="container-fluid px-3 px-xl-4 py-4">@include('transport._nav')<h1>Servicios a generar</h1><div class="card"><div class="card-body"><h4>{{$schedule->name}}</h4><p>{{$schedule->route->name}} · Total: <strong>{{count($preview)}} servicios</strong></p><div class="row">@foreach($preview as $item)<div class="col-md-3 mb-2"><div class="border rounded p-2">{{$item['scheduled_start_at']->format('d/m/Y — H:i')}}<br><small>Hasta {{$item['scheduled_arrival_at']->format('d/m/Y H:i')}}</small></div></div>@endforeach</div>@if(count($preview))<form method="POST" action="{{route('transport.schedules.generate',$schedule)}}">@csrf<button class="btn btn-success">Confirmar y generar</button></form>@else<div class="alert alert-warning">No hay fechas para generar; revise días y excepciones.</div>@endif</div></div></div>@endsection
