@extends('layouts.app')@section('content')<div class="container-fluid px-3 px-xl-4 py-4">@include('transport._nav')<h1>Reportes de Transporte</h1><form method="GET" class="card card-body"><div class="form-row"><div class="col"><label>Desde</label><input type="date" name="from" value="{{today()->startOfMonth()->toDateString()}}" class="form-control" required></div><div class="col"><label>Hasta</label><input type="date" name="to" value="{{today()->toDateString()}}" class="form-control" required></div></div><div class="mt-3"><button formaction="{{route('transport.reports.management','daily')}}" class="btn btn-outline-danger">Informe diario PDF</button><button formaction="{{route('transport.reports.management','weekly')}}" class="btn btn-outline-danger">Informe semanal PDF</button><button formaction="{{route('transport.reports.management','monthly')}}" class="btn btn-outline-danger">Informe mensual PDF</button><button formaction="{{route('transport.reports.management','executive')}}" class="btn btn-outline-danger">Informe ejecutivo PDF</button><button formaction="{{route('transport.reports.issues')}}" class="btn btn-outline-warning">Novedades PDF</button><button formaction="{{route('transport.reports.documents')}}" class="btn btn-outline-warning">Documental PDF</button><button formaction="{{route('transport.reports.indicators.excel')}}" class="btn btn-outline-success">Indicadores Excel</button><button formaction="{{route('transport.reports.history.excel')}}" class="btn btn-outline-success">Histórico Excel</button></div></form></div>@endsection
