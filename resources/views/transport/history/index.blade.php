@extends('layouts.app')@section('content')<div class="container-fluid px-3 px-xl-4 py-4">@include('transport._nav')<h1>Histórico de Servicios</h1><form method="GET" class="card card-body mb-3"><div class="form-row"><div class="col"><input type="date" name="from" value="{{request('from')}}" class="form-control"></div><div class="col"><input type="date" name="to" value="{{request('to')}}" class="form-control"></div><div class="col"><select name="status" class="form-control"><option value="">Todos los estados</option>@foreach(config('transport.service_statuses') as $k=>$v)<option value="{{$k}}" @selected(request('status')===$k)>{{$v}}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-primary">Filtrar</button></div></div></form><table class="table table-striped"><tr><th>Fecha</th><th>Ruta</th><th>Vehículo planificado / real</th><th>Conductor planificado / real</th><th>Salida</th><th>Llegada</th><th>Novedad</th><th>Evidencia</th><th></th></tr>@foreach($services as $s)<tr><td>{{$s->service_date->format('d/m/Y')}}</td><td>{{$s->route_name_snapshot}}</td><td>{{$s->vehicle?->plate??'—'}} / {{$s->actualVehicle?->plate??'—'}}</td><td>{{$s->driver?->display_name??'—'}} / {{$s->actualDriver?->display_name??'—'}}</td><td>{{$s->actual_start_at?->format('H:i')??'—'}}</td><td>{{$s->actual_arrival_at?->format('H:i')??'—'}}</td><td>{{$s->issues->isNotEmpty()?'Sí':'No'}}</td><td>{{['complete'=>'Completa','partial'=>'Parcial','missing'=>'Faltante'][$s->evidence_control['status']]}}</td><td><a href="{{route('transport.services.show',$s)}}">Detalle</a></td></tr>@endforeach</table>{{$services->links()}}</div>@endsection
