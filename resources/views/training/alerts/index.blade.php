@extends('layouts.app') @section('content')<div class="container-fluid py-3">@include('training.partials.nav')<div class="d-flex justify-content-between"><h2>Alertas de capacitación</h2><form method="post" action="{{route('training.alerts.scan')}}">@csrf<button class="btn btn-primary">Actualizar análisis</button></form></div><table class="table"><thead><tr><th>Severidad</th><th>Alerta</th><th>Estado</th><th>Detectada</th><th>Acciones</th></tr></thead><tbody>@forelse($alerts as $a)<tr><td><span class="badge badge-{{$a->severity==='critical'?'danger':'warning'}}">{{$a->severity}}</span></td><td><strong>{{$a->title}}</strong><br>{{$a->message}}</td><td>{{$a->status}}</td><td>{{$a->last_detected_at?->format('d/m/Y H:i')}}</td><td><form method="post" action="{{route('training.alerts.update',$a)}}">@csrf @method('patch')<button name="action" value="acknowledge" class="btn btn-sm btn-outline-warning">Reconocer</button><button name="action" value="resolve" class="btn btn-sm btn-outline-success">Resolver</button></form></td></tr>@empty<tr><td colspan="5">No hay alertas.</td></tr>@endforelse</tbody></table>{{$alerts->links()}}</div>@endsection

