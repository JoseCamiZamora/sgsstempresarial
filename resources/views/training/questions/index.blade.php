@extends('layouts.app') @section('content')<div class="container my-4"><h2>Banco de preguntas</h2>@include('training.partials.nav')@if($errors->any())<div class="alert alert-danger">@foreach($errors->all() as $error)<div>{{$error}}</div>@endforeach</div>@endif<form method="POST" action="{{route('training.questions.store')}}" class="card card-body mb-3">@csrf<select name="training_topic_id" class="form-control mb-2">@foreach($topics as $topic)<option value="{{$topic->id}}">{{$topic->name}}</option>@endforeach</select><textarea name="question_text" class="form-control mb-2" placeholder="Pregunta" required></textarea><select name="question_type" class="form-control mb-2"><option value="single_choice">Selección única</option><option value="multiple_choice">Selección múltiple</option><option value="true_false">Verdadero/falso</option></select><input type="number" step="0.01" name="default_points" value="1" class="form-control mb-2"><textarea name="explanation" class="form-control mb-2" placeholder="Explicación opcional"></textarea><p>Opciones; marque las correctas. Esta información solo se usa en administración.</p>@for($i=0;$i<4;$i++)<div class="input-group mb-1"><div class="input-group-prepend"><div class="input-group-text"><input type="checkbox" name="correct_options[]" value="{{$i}}"></div></div><input name="options[]" class="form-control" placeholder="Opción {{$i+1}}" required></div>@endfor<label><input type="checkbox" name="is_critical" value="1"> Criterio crítico</label><button class="btn btn-primary">Guardar pregunta</button></form><table class="table"><tr><th>Tema</th><th>Pregunta</th><th>Tipo</th><th>Puntos</th></tr>@foreach($questions as $question)<tr><td>{{$question->topic->name}}</td><td>{{$question->question_text}}</td><td>{{$question->question_type}}</td><td>{{$question->default_points}}</td></tr>@endforeach</table></div>@endsection
