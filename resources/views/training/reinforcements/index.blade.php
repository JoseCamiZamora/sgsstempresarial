@extends('layouts.app') @section('content')<div class="container my-4"><h2>Refuerzos de formación</h2>@include('training.partials.nav')@foreach($reinforcements as $reinforcement)<div class="card card-body mb-2"><h5>{{$reinforcement->employee?->nombre_completo}} — {{$reinforcement->attempt->evaluation->topic->name}}</h5><p>Resultado anterior: {{$reinforcement->attempt->percentage_score}}% · Estado: {{$reinforcement->status}}</p>@if($reinforcement->status==='pending')<form method="POST" action="{{route('training.reinforcements.schedule',$reinforcement)}}">@csrf<div class="form-row"><div class="col"><input type="datetime-local" name="scheduled_start_at" class="form-control" required></div><div class="col"><input type="datetime-local" name="scheduled_end_at" class="form-control" required></div><div class="col"><select name="instructor_employee_id" class="form-control">@foreach($employees as $employee)<option value="{{$employee->id}}">{{$employee->nombre_completo}}</option>@endforeach</select></div><div class="col"><input name="location" class="form-control" placeholder="Lugar"></div><div class="col"><button class="btn btn-warning">Programar refuerzo</button></div></div></form>@endif</div>@endforeach</div>@endsection
