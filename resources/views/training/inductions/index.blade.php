@extends('layouts.app') @section('content')<div class="container my-4"><h2>Inducciones SST pendientes</h2>@include('training.partials.nav')<p>Se muestran empleados activos con fecha de ingreso que todavía no tienen una inducción cerrada registrada.</p><form method="POST" action="{{route('training.inductions.store')}}" class="card card-body">@csrf<label>Nuevos ingresos</label><select multiple name="employee_ids[]" class="form-control" required>@foreach($employees as $employee)<option value="{{$employee->id}}">{{$employee->nombre_completo}} — ingreso {{$employee->fecha_ingreso}} — {{$employee->cargo}}</option>@endforeach</select><div class="form-row mt-2"><div class="col"><input type="datetime-local" name="scheduled_start_at" class="form-control" required></div><div class="col"><input type="datetime-local" name="scheduled_end_at" class="form-control" required></div><div class="col"><select name="instructor_employee_id" class="form-control" required>@foreach(\App\Models\Empleado::where('company_id',auth()->user()->company_id)->active()->get() as $employee)<option value="{{$employee->id}}">{{$employee->nombre_completo}}</option>@endforeach</select></div><div class="col"><input name="location" class="form-control" placeholder="Lugar"></div></div><button class="btn btn-primary mt-2">Programar inducción</button></form></div>@endsection
