<!doctype html><html><head><meta charset="utf-8"><style>body{font-family:DejaVu Sans;text-align:center;padding:50px}.box{border:4px double #174a7e;padding:40px}</style></head><body><div class="box"><h1>{{$credential->name}}</h1><p>{{$s->company->razon_social}} — NIT {{$s->company->nit}}</p><p>Hace constar que</p><h2>{{$credential->employee->nombre_completo}}</h2><p>participó en la capacitación interna</p><h2>{{$s->title}}</h2><p>realizada el {{$s->actual_end_at?->format('d/m/Y')}} con duración de {{$s->actual_duration_minutes}} minutos, modalidad {{config('training.modalities.'.$s->modality)}}.</p>@if($credential->credential_type==='internal_approval_certificate')<p>La evaluación interna asociada fue aprobada. Esta constancia no constituye por sí sola certificación profesional ni habilitación legal.</p>@endif<p>Instructor: {{$s->instructorEmployee?->nombre_completo??$s->externalInstructor?->name}}</p><small>Código de verificación: {{$code}}</small></div></body></html>
