@extends('attendance.public.layout') @section('content')<h1 class="h3">Registro de asistencia</h1><h2 class="h5">{{$event->title}}</h2><p>{{$event->company->razon_social}}</p><p><b>Fecha:</b> {{$event->starts_at->format('d/m/Y')}}<br><b>Hora:</b> {{$event->starts_at->format('H:i')}} – {{$event->ends_at->format('H:i')}}</p>@if($errors->any())<div class="alert alert-danger">@foreach($errors->all() as $e)<div>{{$e}}</div>@endforeach</div>@endif<form method="POST" action="{{route('attendance.public.verify',[$event,$token])}}">@csrf<label for="personal_code">Ingrese su código personal</label><input id="personal_code" name="personal_code" class="form-control form-control-lg text-uppercase mb-3" autocomplete="one-time-code" required><button class="btn btn-primary btn-block">Continuar</button></form><p class="small text-muted mt-3">El QR permite acceder al evento, pero no identifica por sí solo a ninguna persona.</p>@endsection
