<div class="table-responsive"><table class="table"><tr><th>Participante</th><th>Rol</th><th>Estado</th><th>Registro</th><th>Acciones</th></tr>@foreach($event->participants as $p)<tr><td>{{$p->name_snapshot}}</td><td>{{$p->role_snapshot}}</td><td>{{$p->record?->status??'Pendiente'}}</td><td>{{$p->record?->confirmed_at?->format('d/m/Y H:i')??'—'}}</td><td>@if($p->record?->signature)<a href="{{route('attendance.signature',$p->record)}}">Firma</a>@endif @if($p->record&&$p->record->status!=='voided')<form class="d-inline" method="POST" action="{{route('attendance.records.void',$p->record)}}">@csrf<input type="hidden" name="reason" value="Corrección administrativa solicitada desde la gestión del evento"><button class="btn btn-sm btn-outline-danger">Anular</button></form>@endif</td></tr>@endforeach</table></div>
